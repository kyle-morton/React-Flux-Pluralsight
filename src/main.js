$ = jQuery = require('jquery'); //access jquery thru '$' or 'jquery' word

var App = console.log('hello world from Browserify');

module.exports = App; //browserify looks for this to export into the bundled JS